import 'package:flutter/material.dart';

class LayoutCard extends StatelessWidget {
  final Widget childWidget;

  LayoutCard(this.childWidget);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
