import 'package:flutter/material.dart';

class SubscribedList extends StatelessWidget {
  const SubscribedList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
